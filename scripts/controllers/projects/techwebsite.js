(function () {
  'use strict';

  /**
   * @ngdoc function
   * @name portfolioApp.controller:TechwebsiteController
   * @description
   * # TechwebsiteController
   * Controller of the portfolioApp
   */
  angular.module('portfolioApp')
    .controller('TechwebsiteController', function ($log, $state) {
      var vm = this;
      $log.debug($state.current);

      //Declare properties here

      //Declare methods here

    });
})();
